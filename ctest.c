#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "pl0.h"

int main(void)
{
    // // char path[30] = "D:\\SJTU\\c\\cifa\\out_test.txt";

    // // FILE* f = fopen(path, "w");
    // // char w_in[10] = "write in";
    // // fwrite(w_in, sizeof(char), sizeof(w_in), f);

    // char a[10] = "abcd.efg";
    // char *b;
    // char *ptr;
    // ptr = strtok(a, ".");
    // printf("ptr=%s\n", ptr);
    // // while (ptr != NULL){
    // //     printf("ptr=%s\n", ptr);
	// // 	ptr = strtok(NULL, ".");
    // // }
    // // printf("%s\n", b);

    char a = "(";
    printf("%s\n", a);

    system("pause");
    return 0;

}
